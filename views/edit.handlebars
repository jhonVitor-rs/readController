<h1>Edite o seu livro:</h1>
<form action="/update/{{book.id}}" method="post" class="form">
  <div class="form-control">
    <label for="title">Título:</label>
    <input type="text" name="title" placeholder="Digite o título do seu livro!" value="{{book.name}}">
  </div>
  <div class="form-control">
    <label for="pagesQtd">Número:</label>
    <input type="number" name="pagesQtd" placeholder="Quantas páginas tem o livro?" value="{{book.pages}}">
  </div>
  <input type="submit" value="Editar" class="button">
  <a href="/" class="button">Voltar</a>
</form>